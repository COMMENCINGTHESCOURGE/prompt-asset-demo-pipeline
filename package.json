{
  "name": "prompt-asset-demo-pipeline",
  "version": "0.1.0",
  "description": "End-to-end demo: writer -> drawer -> sound -> combined output",
  "main": "src/index.ts",
  "bin": {
    "prompt-asset-demo": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "demo": "node dist/cli.ts"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.19.41",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
